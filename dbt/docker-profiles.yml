
moomoo:
  outputs:
    prod:
      type: postgres
      threads: 5
      host: "{{ env_var('MOOMOO_DBT_PG_HOST') }}"
      port: "{{ env_var('MOOMOO_DBT_PG_PORT') | int }}"
      user: "{{ env_var('MOOMOO_DBT_PG_USER') }}"
      pass: "{{ env_var('MOOMOO_DBT_PG_PASSWORD') }}"
      dbname: "{{ env_var('MOOMOO_DBT_PG_DBNAME') }}"
      schema: "{{ env_var('MOOMOO_DBT_SCHEMA') }}"

  target: prod
